function SumAverage(arr1,arr2,arr3){

    var sumLenght = arr1.length + arr2.length + arr3.length;

    var averageLength = Math.floor(sumLenght / 3);

    return `${sumLenght} 
     ${averageLength}`;
}

